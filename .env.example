# ==============================================================================
# AaoCab Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in the values
# Run: cp .env.example .env.local
# ==============================================================================

# ==============================================================================
# SUPABASE CONFIGURATION (Required)
# ==============================================================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
#
# NEXT_PUBLIC_SUPABASE_URL: Your Supabase project URL
# Format: https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# NEXT_PUBLIC_SUPABASE_ANON_KEY: Your Supabase anonymous/public key
# This is safe to expose in the browser - it has limited permissions
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ==============================================================================
# GOOGLE PLACES API (Required)
# ==============================================================================
# Get this from: https://console.cloud.google.com/apis/credentials
#
# Steps:
# 1. Create a project in Google Cloud Console
# 2. Enable the Places API, Geocoding API, Maps JavaScript API
# 3. Create an API key
# 4. Restrict the key to your domains and the Places API
#
# NEXT_PUBLIC_GOOGLE_PLACES_API_KEY: Used for location autocomplete in booking form
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key

# ==============================================================================
# STRIPE CONFIGURATION (Required for payments)
# ==============================================================================
# Get these from: https://dashboard.stripe.com/apikeys
#
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: Public key for Stripe Elements (client-side)
# Starts with pk_test_ (test mode) or pk_live_ (production)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# STRIPE_SECRET_KEY: Secret key for server-side Stripe operations
# Starts with sk_test_ (test mode) or sk_live_ (production)
# NEVER expose this key in client-side code!
STRIPE_SECRET_KEY=sk_test_your_secret_key

# STRIPE_WEBHOOK_SECRET: For verifying Stripe webhook signatures
# Get this from: https://dashboard.stripe.com/webhooks
# Create a webhook endpoint pointing to: https://yourdomain.com/api/payments/webhook
# Select events: payment_intent.succeeded, payment_intent.payment_failed, charge.refunded
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ==============================================================================
# ADMIN DASHBOARD CREDENTIALS (Required)
# ==============================================================================
# These credentials are used to access the admin dashboard at /admin
#
# ADMIN_USERNAME: Username for admin login
ADMIN_USERNAME=admin

# ADMIN_PASSWORD: Password for admin login
# Use a strong password in production!
ADMIN_PASSWORD=your-secure-admin-password

# ADMIN_TOKEN: A secret token used for admin session management
# Generate a random string: openssl rand -hex 32
ADMIN_TOKEN=your-random-admin-token

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# N8N Webhook URL (Optional)
# Used for workflow automation integrations
# NEXT_PUBLIC_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/xxx

# Google Analytics (Optional)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry Error Tracking (Optional)
# SENTRY_DSN=your-sentry-dsn

# Skip Environment Validation (Optional)
# Set to "true" to skip env validation during build (useful for Docker)
# SKIP_ENV_VALIDATION=true

# ==============================================================================
# PRODUCTION CHECKLIST
# ==============================================================================
#
# Before deploying to production:
#
# [ ] Use separate API keys for development and production
# [ ] Use Stripe live keys (pk_live_, sk_live_) in production
# [ ] Set up production Stripe webhook endpoint
# [ ] Use strong admin credentials
# [ ] Never commit .env.local or files with real credentials
# [ ] Add all variables to Vercel dashboard for production
# [ ] Restrict Google API key to production domains
# [ ] Test the complete payment flow in Stripe test mode first
#
# ==============================================================================
